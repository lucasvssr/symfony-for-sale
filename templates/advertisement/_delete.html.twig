<form action="{{ path('app_advertisement_delete', {'advertisement': advertisement}) }}" method="post" onsubmit="return confirm('Are you sure you want to delete ths advertisement')">
    <input type="hidden" name="token" value="{{ csrf_token('delete-advertisement') }}">
    <button type="submit" class="btn btn-outline-danger" >Delete</button>
</form>
